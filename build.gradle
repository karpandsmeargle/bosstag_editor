// Keep `./gradlew build` working from the repository root for CI and local use.
tasks.register('build') {
	dependsOn(':mod:build')
}

tasks.register('check') {
	dependsOn(':mod:check')
}

tasks.register('clean') {
	dependsOn(':mod:clean')
}
